# Bundled layouts

Layout directories in this folder are copied into `./layout` after the standard
TournamentStreamHelper layouts are available. They can reuse the shared fonts,
scripts, styles, and game screenshots from the standard layout package.

Bundled layouts remain part of this repository, so custom tournament designs are
not lost when the standard layout package is downloaded or refreshed.
